## lazy function show
### Arguments:
- bar (`bs.Bossbar?`)
- tick (`int`)
- text (`rawjson`)
### Return:
- bool


Show text in the bossbar with animation according to `tick`. Return true if the animation is finished


